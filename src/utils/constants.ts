// In development, we use the Vite proxy configured in vite.config.ts
// This bypasses CORS by routing requests through our local dev server
export const API_BASE_URL = "/"; 

// The full local path will be: /Epos_Spring/fps/fpstransaction...
